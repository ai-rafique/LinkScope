# Docker Compose reads a .env file next to docker-compose.yml. Copy this to dev/.env
# (git-ignored) to override host ports, e.g. when 1883 is inside a reserved range on
# Windows. Container ports never change; only what the host exposes.
MQTT_PORT=1883
MQTT_WS_PORT=9001
